// This layout is intentionally empty to prevent the default documentation layout from being applied
// The page.tsx will handle its own complete layout

export default function DocumentationLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}